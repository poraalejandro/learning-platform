# Backlog

Ideas y pulido pendiente, sin fase asignada todavía — no implementar hasta que se pida explícitamente.

- ~~**Color del cuadro de pistas**~~ — resuelto: ahora usa el color de acento nuevo en vez de ámbar.
- ~~**Título de la app**~~ — resuelto: renombrada a **PyQuest** (tab, navbar, portada).
- ~~**Rediseño visual general**~~ — resuelto: paleta inspirada en Python (azul/amarillo suavizados), animaciones (línea del árbol dibujándose, confeti al acertar, pop-in), hover/click en botones y nodos.
- ~~**Nuevo tipo de ejercicio: preguntas tipo test**~~ — descartado. Confirmado con el usuario que sería redundante con `match` (unir concepto↔definición ya cubre el caso de uso).
- ~~**Buscador/índice de conceptos**~~ — resuelto (22 sep): página `/search`, enlazada desde el navbar. Compara la búsqueda contra `meta.concepts` y el `prompt` de cada ejercicio (no el JSONB entero, para no dar falsos positivos dentro de código de ejemplo), y contra título/contenido de las lecciones. Los resultados agrupan por nodo; los ejercicios enlazan a `/node/[nodeId]` (respeta el bloqueo), las lecciones directo a `/lessons/[id]`.
- ~~**Sistema de lecciones**~~ — resuelto: `lessons` en Supabase (Markdown), `/lessons` como índice agrupado por nodo, `/lessons/[id]` con `react-markdown` + resaltado de sintaxis. Enlazado desde la página del nodo también. 34 lecciones sembradas, 2-3 por nodo en M2-S6, 1 en M1.
- ~~**Repaso de errores**~~ — resuelto (26 sep): `/mistakes` (general) y `/mistakes?section=python|genai|project|engineering` (por sección), con contador en el navbar. Reutiliza `srs_cards` sin tabla nueva: un ejercicio con tarjeta cuyo `due_date` ya llegó es un error pendiente (solo tipos gating; flashcards y recall siguen repasándose por nodo). "Start review" encadena los ejercicios en orden del árbol con `?review=<sección>` en la URL; al terminar vuelve a `/mistakes`. Los flashcards que marcas "Again" no entran aquí todavía.
- **Aprovechar el campo `meta`**: parcialmente resuelto (22 sep). Hecho: **modo entrevista** (`/interview`, filtra por `interview: true` — 97 ejercicios en M4–S6; M1–M3 no tienen `meta`). Pendiente: estadísticas de aciertos por `concepts`, e intercalar un ejercicio de repaso usando `recuerda_de` del nodo anterior al empezar uno nuevo.
- **Prerrequisitos de las side quests**: hay una propuesta (`s1←m1, s2←m3, s3←m4, s4←m3, s5←m7, s6←m4`) para que se desbloqueen antes de lo que están ahora (`s1←m2, s4←m7, s5←m8, s6←m9`, del seed original). No aplicada — pendiente de decidir si de verdad se quiere adelantar el desbloqueo (2, 4 y 5 nodos antes respectivamente) o se deja como está.
- ~~**Migración completa a inglés**~~ — resuelto (22 sep): interfaz completa + los 154 ejercicios + 34 lecciones + 15 títulos de `skill_nodes` traducidos y verificados, español borrado (no se mantienen los dos idiomas). Identificadores del código Python dentro de los ejercicios también traducidos (`sumar`→`sum_values`, `Grafo`→`Graph`, etc.), no solo el texto. De paso se encontraron y corrigieron 6 bugs reales preexistentes e independientes del idioma: varios tests comparaban un string devuelto contra texto plano, pero el runner real aplica `repr()` primero (añade comillas), así que esos tests siempre fallaban aunque la solución fuera correcta.
