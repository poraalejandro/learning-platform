# Backlog

Ideas y pulido pendiente, sin fase asignada todavía — no implementar hasta que se pida explícitamente.

- **Color del cuadro de pistas**: el fondo ámbar del recuadro de hints se percibe como un color de error/alerta. Cambiar a algo que no se lea como "algo ha ido mal".
- **Título de la app**: sigue siendo literalmente "learning-platform" en todas partes (`<title>` del navegador todavía dice "Create Next App" por defecto, el `<h1>` de las páginas dice "learning-platform"). Falta un nombre real de producto.
- **Rediseño visual general**: paleta de colores más moderna, tipografía, y pulir microinteracciones (hovers, transiciones) — actualmente todo usa los grises/azules por defecto de Tailwind sin una identidad propia.
- **Nuevo tipo de ejercicio: preguntas tipo test (elección múltiple)**. No está en el roadmap de fases del CLAUDE.md (que en la Fase 4 lista `parsons`, `fix_bug`, `match`, `predict_output`) — decidir si es un tipo nuevo (`multiple_choice`) o si encaja como variante de `match`/`predict_output` cuando se diseñe esa fase.
